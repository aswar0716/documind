# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# RAG
langchain==0.3.14
langchain-community==0.3.14
langchain-openai==0.3.1

# Embeddings
sentence-transformers==3.3.1

# Vector store
chromadb==0.6.3

# PDF processing
pypdf==5.1.0

# MLflow
mlflow==2.19.0

# Utilities
pydantic==2.10.5
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1

# Workaround: lark is a missing transitive dependency of the ROS system
# pytest plugin (launch_testing). Installing it prevents pytest from
# crashing at startup on machines with ROS installed.
lark==1.3.1
