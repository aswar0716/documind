# Install base dependencies first: pip install -r requirements.txt
# Then install dev dependencies: pip install -r requirements-dev.txt

# Testing
pytest==9.0.2
httpx==0.28.1        # required by FastAPI TestClient for async support
